name: CI

on:
  pull_request:
    branches: [develop, master]
  push:
    branches: [develop, master]

permissions:
  contents: read

concurrency:
  group: web-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 24
          cache: npm
      - name: Install locked dependencies
        run: npm ci
      - name: Check formatting
        run: npm run format:check
      - name: Lint
        run: npm run lint
      - name: Typecheck
        run: npm run typecheck
      - name: Verify Web V2 architecture
        run: npm run architecture:verify
      - name: Verify Web V2 performance budgets
        run: npm run performance:verify
      - name: Verify Web V2 test strategy
        run: npm run test:strategy
      - name: Unit tests
        run: npm run test:ci
      - name: Runtime server tests
        run: npm run test:server
      - name: Build
        run: npm run build
      - name: Install Chromium for offline shell smoke
        run: npx playwright install --with-deps chromium
      - name: Verify the production shell without a server
        run: npm run test:service-worker
